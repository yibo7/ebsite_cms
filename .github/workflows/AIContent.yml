name: daily-request-3k2k

on:
  schedule:
    # 每天 08:00（北京时间 = UTC+8 → UTC 时间是 00:00）
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  request:
    runs-on: ubuntu-latest
    steps:
      - name: Request 3k2k
        run: |
          curl -I https://www.3k2k.com/api/ai_update
