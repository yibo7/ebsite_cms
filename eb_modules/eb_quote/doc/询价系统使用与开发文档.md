# AI 智能询价系统 — 使用与开发文档

> 模块路径：`eb_modules/eb_quote/`
> 前台入口：`/eb_quote/index.html`
> 后台入口：`/eb_quote/shop_quotes`

---

## 一、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                    用户浏览器 (Frontend)                      │
│   index.html  ←→  app.js (聊天UI + 询价篮)                  │
└─────────────────────┬───────────────────────────────────────┘
                      │ HTTP JSON
┌─────────────────────▼───────────────────────────────────────┐
│              Flask Blueprint (Backend API)                   │
│  ┌──────────────┐    ┌──────────────────────────────┐      │
│  │ quote_apis   │    │  quote_pages (页面渲染)       │      │
│  │ /api/quote/* │    │  /quote/<id> /shop_quotes    │      │
│  └──────┬───────┘    └──────────────────────────────┘      │
│         │                                                    │
│  ┌──────▼──────────────────────────────────────────────┐    │
│  │  AI 供应商 (ai_providers/)   品类处理器 (handlers/)  │    │
│  │  deepseek / joyagent / qwen  printer_drum / clothing│    │
│  └─────────────────────────────────────────────────────┘    │
│         │                                                    │
│  ┌──────▼──────────────────────────────────────────────┐    │
│  │  MongoDB                                            │    │
│  │  NewsContent (商品)  ShopQuoteRecord (报价单)       │    │
│  │  ShopQuotePrompts (提示词配置)                     │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 核心模块文件

| 文件 | 职责 |
|------|------|
| `__init__.py` | 模块入口、蓝图注册、`module_init()`、动态品类下拉 |
| `quote_apis.py` | 所有 API 端点：聊天、搜索、生成报价单 |
| `quote_pages.py` | 页面路由：报价单查看、后台管理、提示词配置 |
| `ai_providers/` | AI 供应商插件（DeepSeek / 京东Joyagent / 阿里千问） |
| `ai_handlers/` | 品类处理器插件（printer_drum / clothing） |
| `datas/quote_record.py` | 报价单数据模型 |
| `datas/prompts_config.py` | 提示词配置数据模型 |
| `templates/` | Jinja2 模板（报价单页、管理页） |

---

## 二、系统使用

### 2.1 后台配置

> 路径：网站后台 → 模块管理 → 智能询价系统

#### AI 供应商配置

| 字段 | 说明 |
|------|------|
| 选择AI供应商 | DeepSeek / 京东Joyagent / 阿里千问 |
| AI供应商密钥 | 对应供应商的 API Key |
| 模型名称 | 如 `deepseek-chat`、`qwen-max` |

#### 品类选择

| 选项 | 说明 |
|------|------|
| 打印复印耗材 | 按品牌+型号精确搜索（适用于鼓芯、粉盒） |
| 服装 | 按品类+颜色+尺码关键词模糊搜索（示例） |

> 切换供应商或品类后**需要重启项目**才能生效。

### 2.2 提示词配置

> 路径：报价单管理 → 提示词配置

允许管理员在不修改代码、不重启的情况下编辑 AI 的提示词：

| 配置项 | 用途 |
|--------|------|
| 店铺名称 | AI 文案中自称的名称 |
| 欢迎语 | 客户打开对话框时显示的欢迎消息（支持 HTML） |
| 提取提示词 | 指导 AI 从用户消息中提取产品参数 |
| 销售文案模板 | AI 撰写回复时使用的模板 |
| 引导提示词 | AI 无法提取参数时引导用户提供更多信息 |

> 修改提示词后**无需重启**，刷新页面即可生效。

### 2.3 前端使用

**前台地址：** `http://your-site/eb_quote/index.html`

**基本流程：**
1. 用户在左侧对话框输入需求（如 "Ricoh MPC8003 鼓芯多少钱"）
2. AI 提取参数 → 搜索数据库 → 返回匹配产品
3. 用户点击产品卡片上的 `[+]` 加入询价篮
4. 在询价篮中修改数量，点击"生成报价单"
5. 生成可分享的报价单链接

**支持的搜索方式：**
- 品牌 + 型号：`"Ricoh MPC8003"`
- 仅品牌：`"佳能鼓芯有哪些"`
- 仅型号：`"MPC8003"`
- OEM/SKU/料号：`"GX-C200-037"`
- 完整描述：`"Drum Unit for Ricoh MP 9000"`

**产品卡片功能：**
- 缩略图 → 点击在新窗口打开商品详情页
- 商品名称 → 点击在新窗口打开商品详情页

---

## 三、API 文档

所有 API 的 Base URL：`/eb_quote/api/quote/`

### 3.1 获取欢迎语

```
GET /welcome
→ { "welcome": "..." }
```

### 3.2 AI 聊天

```
POST /chat
{
    "messages": [
        {"role": "user", "content": "Ricoh MPC8003 报价"}
    ]
}
→ {
    "reply": "<p>...</p>",
    "matches": [
        {
            "title": "Ricoh MPC8003 鼓芯",
            "small_pic": "/uploads/xxx.jpg",
            "unit_price": 25.0,
            "market_price": 30.0,
            "class_name": "打印耗材",
            "sku": "xxx",
            "url": "/a148.html",
            "remarks": "适用机型...",
            "qty": 1
        }
    ]
}
```

### 3.3 生成报价单

```
POST /submit
{
    "sessionId": "uuid",
    "items": [
        {
            "title": "Ricoh MPC8003 鼓芯",
            "small_pic": "...",
            "unit_price": 25.0,
            "market_price": 30.0,
            "class_name": "打印耗材",
            "sku": "xxx",
            "url": "/a148.html",
            "remarks": "...",
            "qty": 5
        }
    ],
    "discount_rate": 0.10
}
→ {
    "code": 0,
    "record_id": "QRxxxxxx",
    "url": "/eb_quote/quote/QRxxxxxx"
}
```

### 3.4 保存询价记录

```
POST /save_record
{
    "sessionId": "uuid",
    "items": [...],
    "summary": "Ricoh MPC8003×5支",
    "total": 112.5
}
→ { "code": 0, "msg": "保存成功" }
```

---

## 四、数据模型

### 4.1 标准化商品数据结构

所有环节（API 返回 / 报价单存储 / 前端渲染）统一使用以下 8 个字段：

```python
{
    "title": "Ricoh MPC8003 鼓芯",      # 商品名称
    "small_pic": "/uploads/xxx.jpg",     # 商品封面图
    "unit_price": 25.0,                  # 单价
    "market_price": 30.0,                # 市场价（从 SKU JSON 提取）
    "class_name": "打印耗材",            # 分类名称
    "sku": "ObjectId字符串",             # SKU / 产品编号
    "url": "/a148.html",                 # 商品详情页链接
    "remarks": "适用机型: MPC8003...",   # 备注/规格说明
}
```

### 4.2 CMS 商品表（NewsContent）

通过 `class_id` 过滤商品分类，各字段含义：

| 字段 | 用途 |
|------|------|
| `id` | 自增整数 ID，用于生成 `/a{id}.html` 详情 URL |
| `title` | 商品标题 |
| `small_pic` | 商品封面图 |
| `column_3` | 品牌（如 Ricoh、佳能） |
| `column_4` | 型号字段 1 |
| `column_5` | 型号字段 2 |
| `column_6` | OEM 码 / 货号 |
| `column_7` | 备注 / 规格说明 |
| `column_10` | SKU JSON 数组（`[{"sku":"...","marketPrice":...}]`） |
| `column_11` | 价格（数值） |

### 4.3 报价单表（ShopQuoteRecord）

| 字段 | 说明 |
|------|------|
| `record_id` | 唯一编号（如 QR65ab...） |
| `session_id` | 浏览器会话标识 |
| `user_id` | 登录用户 ID |
| `items` | 商品数组（存储标准化 8 字段 + qty + subtotal） |
| `status` | `pending_review` → `approved` → `paid` → `completed` / `failed` |
| `total_original` | 原价合计 |
| `total_discount` | 折扣金额 |
| `total_final` | 最终报价 |
| `contact` | 客户联系方式 |

### 4.4 提示词配置表（ShopQuotePrompts）

单例文档 (`profile_key="default"`)：

| 字段 | 说明 |
|------|------|
| `shop_name` | 店铺名称 |
| `welcome_message` | 欢迎语 |
| `extract_prompt` | AI 提取参数提示词 |
| `sales_prompt_tpl` | 销售文案模板 |
| `guide_prompt` | 引导提示词 |

---

## 五、扩展开发

### 5.1 新增品类处理器

在 `ai_handlers/` 下创建新的 Handler 文件，例如 `furniture.py`：

```python
from . import AiHandlerBase, register_ai_handler

@register_ai_handler('furniture')  # 注册名，在后台品类下拉中显示
class FurnitureHandler(AiHandlerBase):
    display_name = "家具"  # 前端显示名称

    # 默认提示词（首次安装时写入 DB）
    default_shop_name = "优选家居"
    default_welcome_message = "欢迎选购家具..."
    default_extract_prompt = """你是一个家具销售助手...
提取字段：
- category: 家具品类（床、沙发、桌子 等）
- style: 风格（现代、中式、北欧 等）
- material: 材质（实木、布艺、皮质 等）
..."""
    default_sales_prompt_tpl = "..."
    default_guide_prompt = "..."

    def search(self, params: dict) -> list[dict]:
        """
        根据 AI 提取的参数搜索商品
        @param params: AI 提取的字段 dict
        @return: 标准化商品列表（8 字段）
        """
        # 搜索逻辑：按品类、风格、材质过滤 CMS 商品表
        # 返回商品列表，使用标准化的 8 字段
        products = []
        for product in matched_products:
            products.append({
                "title": ...,
                "small_pic": ...,
                "unit_price": ...,
                "market_price": ...,
                "class_name": "家具",
                "sku": ...,
                "url": ...,
                "remarks": ...,
            })
        return products

    def build_custom_instruction(self, params: dict, exact: bool) -> str:
        """构建 AI 文案的自定义指令"""
        if exact:
            return "客户明确说了家具品类。热情介绍产品...。"
        return "信息不完整。请客户看看这些是否匹配..."
```

注册后，后台模块配置的品类下拉框**会自动出现**新增的选项。

### 5.2 新增 AI 供应商

在 `ai_providers/` 下创建，实现 `chat()` 方法，详见 `ai_providers/__init__.py`。

### 5.3 搜索逻辑说明

`printer_drum` Handler 的搜索策略：

```
输入参数:
  brand → column_3  (AND 条件)
  model → column_4 / column_5 / title  (AND $or)
  oem   → column_6 / column_7 / column_10 / title  (AND $or)

所有条件组合为 $and，多个条件同时缩小范围。
```

Python 兜底：当 MongoDB `$regex` 因列存储为数组而不生效时，全量查询后在各文本字段中做包含匹配。

---

## 六、常见问题

### Q: 切换品类后之前的提示词会被覆盖吗？
不会。`ensure_seeded()` 仅在数据库首次无配置时写入，切换品类后不会覆盖已有配置。如需重置可手动删除 `ShopQuotePrompts` 集合中的文档。

### Q: 为什么修改提示词后没有生效？
提示词配置无需重启。请刷新页面后重新发送消息。如果仍不生效，检查 `_get_prompt()` 的日志确认是否从 DB 读取到了值。

### Q: 商品搜不到怎么办？
1. 检查商品是否属于正确的 `class_id`（打印耗材：`6852498a0a8b42f2df14146e`）
2. 检查搜索关键词是否在 `column_3`~`column_10` 或 `title` 中
3. 查看后端日志中的 `[DEBUG 兜底搜索]` 确认是否进入兜底逻辑

### Q: 产品 URL 链接为什么打不开？
确保 `NewsContentModel.id` 在数据库中有正确的自增整数值。URL 生成方式为 `url_links.get_content_url(p.id)` → `/a{id}.html`。

---

## 七、技术栈

| 技术 | 用途 |
|------|------|
| Python 3 + Flask | Web 框架 |
| MongoDB + pymongo | 数据库 |
| Jinja2 | 模板渲染 |
| DeepSeek / 千问 API | AI 对话 |
| HTML + CSS + Vanilla JS | 前端 |